# ── GAS Backend（主要；註冊、審核、Email 密碼帳號皆使用）──
GAS_WEB_APP_URL=
GAS_API_TOKEN=
GAS_WORKSPACE_ID=WS-YH-115
GAS_SPREADSHEET_ID=

# ── 永和區管理者（Gmail 允許清單，不需 OAuth）──
GOOGLE_ALLOWED_EMAILS=yongheelderly0826@gmail.com
GOOGLE_OWNER_EMAILS=yongheelderly0826@gmail.com

# ── Supabase（完全選用；留空時 GAS 使用者流程仍可完整運作）──
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# 預設為 GAS；只有要切回 Supabase 使用者管理時才填 supabase。
USER_MANAGEMENT_BACKEND=gas

# ── App ──
NEXT_PUBLIC_APP_URL=https://elder-visit-platform.vercel.app

# ── 安裝精靈（v2/v3，僅本機或受信任環境）──
INSTALLER_ENABLED=false
INSTALLER_SECRET=
INSTALLER_RUNNER=local
GH_TOKEN=
