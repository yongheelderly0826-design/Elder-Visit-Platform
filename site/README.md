# GitHub Pages 入口

公開登入網址（部署完成後）：

https://yongheelderly0826-design.github.io/Elder-Visit-Platform/

兩位同事用同一帳號 `yongheelderly0826@gmail.com` 進入，資料仍寫入同一份 Google 試算表。

首次進入需貼上 `GAS_API_TOKEN`（與本機 `.env.local` 相同），瀏覽器會記在本機，不會上傳 GitHub。
